# 电赛小车迁移说明

## 当前运行结构

`main.c` 初始化并运行以下模块：

1. `USART1`：3 个地址式步进电机驱动器。
2. `USART2`：JY61P 三轴 IMU（复用原 HWT101 协议解析器）。
3. `UART5`：保留的双误差视觉接口。
4. `USART6`：第二题位置误差/速度视觉接口，PC6/PC7。
5. `PE11、PE10、PE9、PE8、PE7、PA6、PA11、PA7`：从左到右 8 路数字红外。
6. `PB6、PB7`：题号选择键和确认键，低电平按下。
7. `PB8、PB9`：OLED 硬件 I2C1。
8. `TIM2`：1 ms 基础时钟。
9. `DS.c/.h`：统一硬件映射和数据入口。
10. `DS_task.c/.h`：选题、启动和任务状态机。
11. `LineFollow.c/.h`：第一题循迹控制。
12. `BalanceControl.c/.h`：第二题球位置外环和杆角内环串级控制。
13. `Task4PositionControl.c/.h`：第四题独立的球位置外环和电机位置模式控制。
14. `MotorPositionMonitor.c/.h`：第二/四/九题共享的 3 号电机非阻塞位置读取。

主循环持续调用 `DS_Run()` 和 `DS_Task_Run()`，不能在任务里加入长时间
阻塞延时，否则会降低按键扫描和循迹控制频率。

## 当前硬件映射

| 模块 | 映射 |
|---|---|
| 左轮步进电机 | USART1，总线地址 `0x01` |
| 右轮步进电机 | USART1，总线地址 `0x02`，软件方向反相 |
| 平衡架步进电机 | USART1，总线地址 `0x03` |
| JY61P（复用 HWT101 协议解析器） | USART2，9600 |
| 原双误差视觉接口（保留） | UART5，9600 |
| 第二/四题小球视觉 | USART6，PC6=TX、PC7=RX，115200 |
| 红外 1~8（从左到右） | PE11、PE10、PE9、PE8、PE7、PA6、PA11、PA7 |
| 红外有效电平 | 默认低电平有效 |
| 按键 1（选题） | PB6，低电平按下，内部上拉 |
| 按键 2（确认/启动/停止） | PB7，低电平按下，内部上拉 |
| OLED | I2C1，PB8=SCL、PB9=SDA，AF4 开漏复用，100 kHz |

电机地址、方向和红外引脚集中在 `Core/Inc/DS.h`。按键映射在
`Core/Inc/button.h`，OLED 外设映射在 `Core/Src/i2c.c` 和
`Core/Src/stm32f4xx_hal_msp.c`。接线改变时应同步修改相关源文件和 `gc.ioc`。

PC6/PC7 已重新配置为 USART6，但只复用串口硬件，不恢复原舵机协议和舵机
任务。按键已迁移到 PB6/PB7。

## DS 接口

- `DS_ChassisSetSpeed(left, right, slope)`：两轮差速速度控制，正值表示小车
  前进方向。
- `DS_ChassisStop()`：左右轮分别立即停止，命令间隔 2 ms，并补发两轮以避免
  一圈结束时单个停止帧丢失后仍有一个车轮持续转动。
- `DS_BalanceSetSpeed(speed, slope)`：平衡架步进电机速度模式。
- `DS_BalanceMoveRelative(pulses, speed, acceleration)`：平衡架相对位置模式。
- `DS_BalanceMoveAbsolute(pulses, speed, acceleration)`：平衡架绝对位置模式。
- `DS_BallVisionIsFresh()`：检查第二题位置量在最近 200 ms 内是否有效。
- `DS_IR_ReadRaw()`：8 路实际 GPIO 电平，bit0 对应红外 1。
- `DS_IR_ReadActive()`：按有效电平转换后的检测结果。
- `DS_GetState()`：统一读取红外、视觉和陀螺仪数据。

UART5 保留原双误差协议：

```text
x_error,y_error\n
none\n
```

有效视觉数据超过 200 ms 未更新时，`vision_valid` 会自动清零。

USART6 第二题在按键确认后由 STM32 发送 `c2`，视觉随后以 50 Hz 发送：

```text
B,-27,18\n
none\n
```

两个数分别是中心位置误差（参考像素）和轴向速度（参考像素/秒），视觉
右侧均为正。480 宽检测通道输出前乘 `CONTROL_OUTPUT_SCALE`，保持原 640 宽
标定。每次平衡电机控制/停止命令后，STM32 通过同一全双工 USART6 异步回传：

```text
F,seq,mcu_ms,vision_frame,vision_age_ms,position_x10,velocity_x10,
  error_x10,p_x100,i_x100,d_x100,motor_command,motor_status\n
```

反馈使用中断发送；若上一帧尚未发送完成则丢弃本帧并让 `seq` 留下缺口，
不阻塞控制。P/I/D 字段现在表示球位置外环产生的三个目标杆角分量，单位为
0.01°；第二题的 `motor_command` 是最终 RS485 速度命令，第四题则是最终
RS485 绝对位置目标脉冲。手动停止时 STM32 等待
最后一帧最多 20 ms，然后发送 `ok`，视觉停止输出。

## DS_task 操作

- `DS_Init()` 完成后、进入菜单前，`main.c` 必须让 3 号电机执行一次
  `+240` 脉冲、速度 50、加速度 10 的相对运动。它是把机构送到大致中间
  位置的必要上电预定位，不是回零或遗留测试命令；除非机械启动要求明确
  改变，否则不要删除、反向或修改该命令。题目 9 在该预定位之后进行循环
  往复和数据采集，但不把任何结果自动写入题目 2。
- 上电进入选题菜单，题号初始显示为 0。
- 每按一次按键 1，题号按 `1 → 2 → ... → 9 → 1` 循环。
- 选择题号 1 后按按键 2，开始循迹并从 0 计时。
- 第一题运行期间再次按按键 2，会立即停车并保留最终时间。
- JY61P 解绕累计偏航角达到一圈阈值后，会自动停车并保留最终时间。
- 选择题号 2 后按按键 2，启动 USART6 视觉、非阻塞 `0x36` 电机位置读取和
  串级控制，并把小球目标设为中心 `0`。外环把球位置/速度变为目标杆角，
  内环根据实际杆角产生速度命令。动作命令后保留 2 ms 总线换向时间，底层会
  丢弃混入位置接收的四字节动作确认帧并重新对齐标准 8 字节 `0x36` 回包。
  短时丢包沿用最近位置，连续 120 ms 无新位置才停机；视觉失效时杆回到水平。
- 第二题当前只持续调节到中心，不自动结束；再次按按键 2 会立即停止 3 号
  电机、停止位置查询并发送 `ok`。提交 `a010e37` 的稳态拟合已经把
  `rod_angle_per_motor_degree` 更新为 `-0.36450137`，并将配套方向更新为
  `tilt_direction=+1`、`motor_direction=-1`。该数据的绝对零点只属于当次
  相对角采集，因此任务 2 仍要求启动前物理调平并捕获本次水平锚点。根据
  `-8.6..+6.6°` 的拟合验证范围，最终目标角上限/保护角限幅分别为 `6.0°`
  和 `6.5°`。当前首轮内外环调试值进一步限制为目标角 `1.5°`、电机速度
  `30`、slew `8`，并关闭两个外环积分。角度内环和外环参数继续集中在
  `balance_control_config`，后续再接入 `+5 cm -> -5 cm` 分段状态机。
- 选择题号 4 后按按键 2，复用同一 `c2` 视觉流和球位置外环，但使用独立的
  `task4_position_control_config/state`。启动后的第一个有效 `0x36`
  位置作为本次水平锚点；目标管角先经 `-0.0020022658 °/count` 映射为电机
  位置计数，再按 `20.48 count/pulse` 换成幂等的 `0xFD` 绝对位置目标。
  驱动器内部位置环的速度为 `2`、加速度档为 `0`。`0xFD` 绝对位置目标每
  20 ms 连续刷新。按下确认后 `0x36` 先按 5 ms 周期快速取得第一次有效位置并
  建立水平锚点，随后自动降到 200 ms 周期低频更新位置显示；建立后查询短时
  失败不再中断位置命令。第四题不使用也不
  修改第二题的角度内环 Kp、速度死区、最小速度、slew、零点或积分器。视觉
  失效时回到本次水平目标；PB6 可像第二题一样关闭/恢复周期 OLED 刷新，PB7
  停止控制并发送 `ok`。
- 选择题号 9 后按按键 2，进入独立的 3 号电机位置与 IMU 数据采集页。
  监视器每 20 ms 发送 `03 36 6B`，以 50 Hz 读取原始位置 `P` 和换算角度；
  该周期只用于题目 9，不改变题目 2 的位置读取参数。
  `DS_TASK_Q9_UPPER_PULSES` 和 `DS_TASK_Q9_LOWER_PULSES` 分别设置任务启动
  稳定位置上方和下方的端点幅度，当前分别为 210 和 150。确认后先等待上电
  `+240` 和残余运动稳定，再把该位置及当时的 IMU 姿态作为采集参考。第一次
  运动为 `+上幅度`；检测到电机位置确实变化、随后连续 15 次位置更新稳定
  （仍约 300 ms），并在端点停留 500 ms 后，才用 `上幅度+下幅度` 反向
  跨越两个端点。禁止按固定周期提前反向；单次运动超过 5 秒仍未稳定会停机
  并报告超时。USART6 同时以 50 Hz 保留发送 `Q9,...\n` 遥测，包含电机
  位置、相对三轴角、有效状态和最近运动状态。该任务不启动第一题或第二题
  控制器，也不修改
  `balance_control_config`；再次按按键 2 会停止位置查询、遥测和 3 号电机。
- 题目 5~8 暂时显示 `NOT READY`；按按键 2 返回选题菜单。

OLED 菜单和第一题页面的第 4 行显示 HWT101 数据：`Y:` 为偏航角，`Z:`
为 Z 轴角速度，末位 `V` 表示数据有效，`X` 表示超过 200 ms 未收到新数据。
第二题页面显示接收误差 `E`、接收速度 `V`、电机命令 `M`、相对水平杆角
`A`、视觉有效标记 `RX:V/X`、位置有效标记 `P:V/X`、稳定标记 `S:V/X`
和运行时间。
第四题页面的状态标记、`E`、`V`、`A` 和时间含义相同，`C` 显示发送给
驱动器的有符号绝对位置目标脉冲。
第九题正常页面显示电机原始位置、相对稳定启动姿态的 X/Y/Z 三轴角、
位置读取状态、更新次数和最近运动方向；位置通信失败时显示原始回包诊断页。
自动停车默认使用 350° 累计净偏航角、3 秒最短运行时间和 3 帧确认；按键 2
仍可随时手动结束。相关参数位于 `line_follow_config`。

## OLED 移植结果

拖入的 OLED 驱动原本是 STM32F103 标准外设库软件模拟版本，现已改成
STM32F407 HAL 硬件 I2C1，并加入 Keil 工程。默认使用常见 SSD1306 地址 `0x3C`
（写地址字节 `0x78`）。

I2C1 使用 PB8/PB9 的 AF4 开漏复用和 HAL 阻塞发送，速率为 100 kHz。初始化
通过硬件 ACK 自动探测 `0x3C` 和 `0x3D`。
可在 Keil Watch 中直接观察 `ds_task.oled_ready`：1 表示收到 ACK，0 表示
初始化通信失败。`ds_task.oled_address` 显示识别到的 7 位地址（`0x3C` 或
`0x3D`）。上电成功通信后会先全屏点亮 1 秒，再显示任务菜单。

若屏幕仍不亮，应依次检查：

1. VCC/GND、PB8/PB9 接线和共地。
2. 模块是否确实为 4 针 I2C SSD1306，而不是 SPI 或 SH1106 特殊版本。
3. 在 Watch 中检查 `ds_task.oled_ready`：0 表示两个地址都没有成功完成通信。
4. SCL/SDA 是否有外部上拉。代码启用了内部上拉，但长线或高速边沿建议使用
   约 4.7 kΩ 外部上拉。

## 模块处理结果

| 处理 | 模块 | 原因 |
|---|---|---|
| 复用并修正 | `zhangdatou` | 底层步进协议可复用；已修正 13 字节位置命令和超时 |
| 复用并简化 | `HWT101` | 陀螺仪仍需要；已改为完整 11 字节校验 |
| 复用并解耦 | `serial` | 保留视觉逐行接收，输出改接到 DS 状态 |
| 复用并移植 | `OLED` | 从 F103 标准库软件模拟改为 F407 HAL 硬件 I2C1 |
| 重写并启用 | `button` | 改为 PB6/PB7 双按键、20 ms 非阻塞消抖 |
| 新增 | `DS_task`、`LineFollow` | 选题/发车状态机和第一题循迹 |
| 新增 | `BallVision`、`BalanceControl` | USART6 误差/速度接收和第二题串级平衡控制 |
| 新增并复用 | `MotorPositionMonitor` | 第二题杆角反馈和第九题数据采集共享同一非阻塞读取器 |
| 保留 | `PID` | 通用 PID 库保留；第二题采用独立的 dt 感知、增益调度串级控制 |
| 暂时保留但不初始化 | `WS2812`、`screen`、`encoder_f407` | 可能用于状态提示、调试或里程反馈 |
| 删除 | `GC_task`、`action` | 原物流赛任务流程 |
| 删除 | `servo`、`LobotServoController`、`huaner_servo`、`ServoMotorControl` | 新车无舵机 |
| 删除 | `QRcode`、`laser`、`bluetooth` | 新需求不使用，且二维码/蓝牙会与视觉争用 UART5 |
| 删除并替换 | `Motor_Move`、`GC_Chassis_Control` | 四轮麦克纳姆和 Z 轴专用，已由 DS 两轮/平衡架接口替换 |

## 上车前必须确认

1. 架空底盘，确认 `DS_ChassisSetSpeed(200, 200, ...)` 时两轮都让小车向前。
2. 用黑线逐个扫过红外 1~8，确认 OLED 的 `IR:` 从左到右对应且黑线显示 1。
3. 确认按键未按时 PB6/PB7 为高电平，按下为低电平。
4. 先用较低速度完成循迹方向检查，再逐步提高速度和 P 参数。
5. 若题目 9 数据用于分析杆角关系，JY61P 应刚性固定在随杆运动的部分、
   靠近转轴但不要直接贴在电机上，推荐 PCB 平面与杆平行、X 轴沿杆长方向。
   同一颗 IMU 还承担第一题车体偏航；若永久装杆，第一题期间必须保证杆不
   相对车体摆动，理想方案是车体/杆各一颗 IMU。
6. 运行题目 9 采集多个完整上下端循环并保存 `Q9` 遥测，同时记录本次使用的
   `DS_TASK_Q9_UPPER_PULSES`、`DS_TASK_Q9_LOWER_PULSES`、速度、端点停留时间
   和稳定判据。题目 9 不自动标定或覆盖题目 2 参数；提交 `a010e37` 的拟合
   已经人工复核并写入当前带符号比例、方向和安全限幅，后续数据仍须走相同
   的离线复核流程。
7. 架空或托住平衡架，以较小角度/速度限幅制造正负视觉误差，分别确认
   `tilt_direction`、`motor_direction` 和 `rod_angle_per_motor_degree`
   的符号；一次只修改一个方向参数。

循迹算法和调参顺序见 `LINE_FOLLOW_TUNING.md`；第二题见
`BALANCE_CONTROL_TUNING.md`。
